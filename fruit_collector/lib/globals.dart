library com.example.fruit_collector.globals;

Duration timerDuration = Duration(seconds: 90);
int points = 0;

//this is a globals library that keeps track of variables I need to use in multiple .dart files